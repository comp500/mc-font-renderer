import {FontRenderer, FontStyle} from "./main";
import {TooltipRenderer} from "./tooltip";

(window as any).FontRenderer = FontRenderer;
(window as any).FontStyle = FontStyle;
(window as any).TooltipRenderer = TooltipRenderer;